

def Cookie(request):
    """
    Get cookie value(s).

    Set `request.cookie` with Cookie object.
    """
    pass


def Session(request):
    """
    Restore session based on Cookie session_id.

    Set `request.session` with LazySession to fetch data from
    database when we access it.
    """
    pass
